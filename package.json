{
  "scripts": {
    "test": "jasmine-node --verbose specs",
    "test-single": "jasmine-node --verbose ",
    "jest": "node_modules/.bin/jest"
  },
  "dependencies": {
    "jasmine-node": "^3.0.0",
    "jest": "^24.8.0"
  }
}
